# Versiyalar birgalikda hal qilingan (pip resolver) va sinovdan o'tgan.
# Yangilashda `pip install -U` dan keyin botni ishga tushirib tekshiring.

# Telegram Bot
aiogram==3.30.0
aiohttp==3.10.11

# Database
SQLAlchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# Alembic va migratsiya skriptlari uchun sync drayver
psycopg2-binary==2.9.10

# Redis — kesh, rate-limit, FSM
redis==5.2.1

# Konfiguratsiya
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2

# Tarjima
deep-translator==1.11.4
# Til aniqlash: lokal, tarmoqsiz. `translations.source_lang_detected` uchun.
py3langid==0.3.0

# Ovoz (TTS)
edge-tts==7.2.8
